# Copiez ce fichier vers .env (gitignoré) :
#   cp .env.example .env
#
# Utilisé par docker-compose pour PostgreSQL.

POSTGRES_DB=invoiceai_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_POSTGRES_PASSWORD
